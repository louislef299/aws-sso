run:
  timeout: 5m
issues:
  exclude:
    - gopath
    - scripts
    - vendor
linters:
  disable-all: true
  enable:
    - gofmt
    - goimports
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused
    - goheader